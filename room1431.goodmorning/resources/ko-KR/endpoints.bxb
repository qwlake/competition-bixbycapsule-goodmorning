endpoints {
  action-endpoints {
    action-endpoint (CalculateSleepTime) {
      accepted-inputs (wakeupTime)
      local-endpoint (calculateSleepTime.js)
    }

    action-endpoint (CalculateWakeupTime) {
      accepted-inputs (sleepTime)
      local-endpoint (calculateWakeupTime.js)
    }
  }
}
