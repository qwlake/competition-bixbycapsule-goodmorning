action (PeriodHours) {
  description (SleepPeriodHours 연결)
  type (Search)
  collect {
    input (sleepPeriodHours) {
      type (SleepPeriodHours)
      min (Required) max (One)
    }
  }
  output (SleepPeriod)
}
