action (FindRoute) {
  description (대중교통을 이용한 경로를 찾는 액션)
  type (ArchiveLookup)
  collect {
    input (startPosition) {
      type (StartPosition)
      min (Required) max (One)
    }
    input (endPosition) {
      type (EndPosition)
      min (Required) max (One)
    }
  }
  output (Results)
}
