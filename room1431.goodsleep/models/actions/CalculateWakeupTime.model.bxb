action (CalculateWakeupTime) {
  type (Search)
  description (지금 자면 몇 시에 개운하게 일어나는지)
  collect {
    input (sleepTime) {
      type (SleepTime)
      min (Required) max (One)
    }
  }
  output (ResultWakeups)
}
